import AetherFlowHero from "@/components/ui/aether-flow-hero";

// Standalone reference demo for the integrated component → /aether-demo
export default function DemoOne() {
  return (
    <main className="App bg-black">
      <AetherFlowHero />
    </main>
  );
}
