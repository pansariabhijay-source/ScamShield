# Heavy ML dependencies — only needed when ENABLE_HEAVY_MODELS=true.
# Kept separate to keep the API image small and cold-starts fast.
-r requirements.txt

# Data prep + training of the shipped text model (scripts/fetch_datasets.py,
# scripts/train_text_model.py). Not needed to *serve* the model.
pandas==2.2.3
requests==2.32.3

transformers==4.47.1
torch==2.5.1
sentence-transformers==3.3.1
xgboost==2.1.3
easyocr==1.7.2
pillow==11.0.0
numpy==2.2.1
