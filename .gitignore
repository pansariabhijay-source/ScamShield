__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
env/
.env
.env.*
!.env.example
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
*.log
.DS_Store
.idea/
.vscode/
dist/
build/

# Training datasets — large & regenerable via `python -m scripts.fetch_datasets`.
# The trained model artifact (app/detectors/models/*.joblib) IS committed so the
# service runs out of the box without a training step.
data/raw/
data/processed/
# …but the curated, hand-authored India seed IS source data — keep it.
!data/curated/
!data/curated/**
